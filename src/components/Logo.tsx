import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <div className="text-primary font-inter-extrabold text-xl">
            <Link to={"/"}>Rupinder Singh</Link>
        </div>
    );
};

export default Logo;
